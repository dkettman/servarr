apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: audiobookshelf-claim2
  name: audiobookshelf-claim2
  namespace: servarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
